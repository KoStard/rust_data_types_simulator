pub struct Queue {
    mem: Vec<i64>
}

// FIFO - first-in-first-out
// Has to do operations in O(1) time
impl Queue {
    fn new() -> Queue {
        Queue{
            mem: vec![]a
        }
    }

}
